"use client";

import { ExchangeTabs } from "./Tabs";


const ExchangeContainer = () => {
  

  return (
    <section className="max-h-[100vh] my-5 m-auto w-[90vw] lg:w-[70vw]">
      <ExchangeTabs />
    </section>
  );
};

export default ExchangeContainer;
